# Lesson 6: Essay Writer — workbooks/lesson-06-essay-writer.ipynb
#
# From project root:
#   source .venv/bin/activate
#   pip install -r requirements-lesson-06.txt
#
# SQLite checkpoints: `langgraph-checkpoint-sqlite` (SqliteSaver).
# In-notebook runs use `with SqliteSaver.from_conn_string(":memory:") as memory:` (course pattern).
# `workbooks/helper.py` uses `sqlite3.connect(..., check_same_thread=False)` for Gradio.

langgraph>=0.2.0
langgraph-checkpoint-sqlite>=2.0.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
tavily-python>=0.5.0
python-dotenv>=1.0.0
httpx>=0.27.0
ipykernel>=6.0.0
gradio>=4.0.0,<5
# Gradio 4.x imports huggingface_hub.HfFolder; it was removed in newer hub releases.
huggingface_hub>=0.20.0,<0.26.0
pydantic>=2.0.0
