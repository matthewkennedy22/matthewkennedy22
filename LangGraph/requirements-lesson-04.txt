# Lesson 4: Persistence and Streaming — workbooks/lesson-04-persistence-streaming.ipynb
#
# From project root:
#   source .venv/bin/activate
#   pip install -r requirements-lesson-04.txt
#
# SQLite checkpoints live in the `langgraph-checkpoint-sqlite` package (SqliteSaver + AsyncSqliteSaver).
# Async saver needs `aiosqlite`.

langgraph>=0.2.0
langgraph-checkpoint-sqlite>=2.0.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
tavily-python>=0.5.0
python-dotenv>=1.0.0
httpx>=0.27.0
aiosqlite>=0.19.0
ipykernel>=6.0.0
