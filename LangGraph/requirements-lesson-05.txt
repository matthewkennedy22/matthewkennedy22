# Lesson 5: Human in the Loop — workbooks/lesson-05-human-in-the-loop.ipynb
#
# From project root:
#   source .venv/bin/activate
#   pip install -r requirements-lesson-05.txt
#
# SQLite checkpoints: `langgraph-checkpoint-sqlite` (SqliteSaver).
# The notebook keeps an open `sqlite3` connection for `:memory:` so state survives across cells.

langgraph>=0.2.0
langgraph-checkpoint-sqlite>=2.0.0
langchain-core>=0.3.0
langchain-openai>=0.2.0
langchain-community>=0.3.0
tavily-python>=0.5.0
python-dotenv>=1.0.0
httpx>=0.27.0
ipykernel>=6.0.0
